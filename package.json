{
  "name": "starter",
  "version": "1.0.0",
  "description": "starter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server",
    "compile:sass": "node-sass --watch sass -o css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lite-server": "^2.6.1"
  },
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
